# Lint / type toolchain, mirroring the house set. Versions pinned so a tool
# release cannot turn CI red on an unchanged commit. The suites are stdlib
# only; the extracted utility deliberately has no runtime dependencies.
pycodestyle==2.14.0
pylint==4.0.6
pyright==1.1.408
